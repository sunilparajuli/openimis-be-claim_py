# from django.db import connection


class claim_submit(object):
    pass

def submit_claims(claim_submits):
    # cur = connection.cursor()  
    # try:
    #     params = ()
    #     cur.execute("{CALL uspSubmitClaims (?)}", params)
    # finally:
    #     cur.close()
    return None